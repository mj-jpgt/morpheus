# Vendored box evidence: byte-identical to the originals on ubuntu@150.136.45.194,
# sha256-verified against MANIFEST.json by
# v2/tests/test_p2_figures.py::test_vendored_data_matches_its_manifest.
# Line endings must NOT be converted on checkout or the digests stop matching and
# the provenance claim this directory exists to support becomes false. Same rule,
# and same reason, as v2/research/rebase/p2/collapse_tracks/.gitattributes.
* -text
