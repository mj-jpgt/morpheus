## 2026-08-03 14:00 UTC — Both D1 arms clear G2.6; D1 launched, six runs, three concurrent

**Logged:** 2026-08-03 14:00 UTC. **How obtained:** `gate_check.py` / `gate_check_only.py` and
`morpheus.v2.research.rebase.phase_d d1 --execute` on the A100 (`150.136.45.194`). Run root
`~/e0_run/d1_v1` (persistent storage). Logs in `~/e0_run/d1_diag/` and `~/e0_run/d1_v1_launch.log`.

### Technical

**Entry condition discharged. Both arms, three seeds, the actual gate functions and predicates,
criteria and step budget unchanged.**

| seed | `programme_free` (needs ≤ 0.10) | consistency (≤ 0.02) | reduction (≥ 0.80) | `programme_only` (needs ≤ 0.10) | reduction |
|---|---|---|---|---|---|
| 42 | **0.01871** | 0.00159 | 0.996 | **−3.82884** | 3.752 |
| 43 | **0.01206** | 0.00274 | 0.997 | **−4.26766** | 4.686 |
| 44 | **0.05666** | 0.00265 | 0.988 | **−2.95128** | 2.023 |

Chance for the contrastive arm is ln 16 = 2.7726; it read 2.63 / 2.44 / 2.66 as shipped.

**Robustness caveat, recorded because it is real.** The gate's 800-step budget is marginal for some
batch compositions. Re-running the contrastive arm at token budget 32768 instead of 8192 — which
changes which 16 patients form the fixed batch — gives 0.6304 / 0.0081 / 0.0108, i.e. seed 42 fails
at 800 steps. It is **slow descent, not a stall**: the same seed and batch reach 0.01847 at 2400
steps. The step budget was deliberately left at 800 rather than raised to make a seed pass; the
marginality is reported instead. D1 is running at token budget 8192, where all three seeds clear the
threshold at the unchanged budget.

**D1 configuration.** Token budget 8192 and programme head dim 256, matching what D2 actually ran
(`D2_PAIR_MANIFEST.json`) rather than the handoff's 32768 — the two arms are identical to each other,
which is what `run_d1` asserts and what the ablation requires. 40 epochs, lr 2e-4, weight decay 1e-2,
decorrelation 0.04, variance 0.01, separation 0.01, warmup 4, `--restrict-to-split`, no MLP-CLIP
anchor and no teacher in either arm (D1.3). Seeds 42/43/44 × arms P/F = six runs, executed three at a
time via the new `--max-parallel`; previously `run_d1` serialised all six despite their being
independent processes writing to disjoint directories.

**Test status at launch.** Root suite 59 passed. The three v2 files touching the changed code —
`test_programme_free.py`, `test_phase_d_pairing.py`, `test_stress_collapse.py` — 31 passed, including
three new tests pinning the centring behaviour and the fact that G2.6 still grades the *uncentred*
number. The full 168-test v2 suite was still running at launch, starved by another agent's
concurrent CALIBRA/joblib workload (box load ~130 on 30 cores); it was stalled on
`test_e0_basis_transfer.py`, which touches none of the changed code.

### In plain terms

The health check both experimental arms have to pass before they are allowed to spend GPU time now
passes for both, on all three repeats, with the pass mark exactly where it was. The arm that had
never successfully trained anywhere on this project — a biology head taught without any curated
programme supervision — clears the bar by a factor of two to eight.

One honest wrinkle: the check gets 800 attempts to memorise sixteen patients, and for some
draws of those sixteen that is just barely enough. Given 2400 attempts it clears comfortably. The
budget was left alone rather than raised to flatter a result.

### Meaning for the claim

D1 is now running and will, for the first time, produce the arm `PHASE1B_TARGETED_READOUT.md` says
exists nowhere on disk. Nothing scientific is licensed yet — the run must finish, its CALIBRA G4
controls must pass, and the Stage-1 audit checks must be applied to it before any D1 number is
quoted.

### Files / commits

- `c80bc08`, `c20f908`, `04aa439`, `1fa226e`
- `~/e0_run/d1_v1/D1_PAIR_MANIFEST.json`, `D1_LAUNCH_PLAN.json`
- `~/e0_run/d1_diag/gate4_*.log` (programme_free), `gateP3_*.log` (programme_only), `gate32k_*.log`
