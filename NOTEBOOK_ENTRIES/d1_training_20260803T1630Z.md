## 2026-08-03 16:30 UTC — D1 is training: the in-runner G2.6 passed at 2400 steps, including the seed that failed at 800

**Logged:** 2026-08-03 16:30 UTC. **How obtained:** `~/e0_run/d1_v1/*/train_metrics.jsonl` and
`~/e0_run/d1_v1_launch.log` on the A100 (`150.136.45.194`).

### Technical

The relaunched D1 cleared G2.6 inside the runner — the ground-truth invocation, not the harness — on
all three concurrently-executing runs, `d1_p_seed42`, `d1_f_seed42` and `d1_p_seed43`. `d1_f_seed42`
is the `programme_free` arm that aborted at 0.19540 under the 800-step budget; at 2400 it passes and
is training. Zero `RuntimeError`s in the launch log.

First recorded epoch of `d1_f_seed42`, i.e. real training rather than the memorisation clone:

```
train_biology_contrastive                        6.1524   (raw, uncentred, 4096-key queue)
train_biology_contrastive_centred                5.8643
train_biology_contrastive_effective_negatives    2575     (chance = ln 2576 = 7.854)
train_biology_full_consistency                   0.0574
train_biology_feature_std                        0.0337
train_loss                                       5.9218
gradient_cosine(contrastive, full_consistency)  -0.0605
```

Two things worth noting. The contrastive term is already **below chance** at epoch 0 against 2575
effective negatives, which is what a working objective should look like and is not what this arm did
before. And the new `biology_contrastive_centred` metric is reported alongside the raw one, so the
gap between the optimised and the graded quantity is visible in every epoch of the training record
rather than having to be reconstructed.

The mildly negative gradient cosine between the contrastive term and `biology_full_consistency`
(−0.06) is consistent with the isolation result that the consistency term at weight 1.0 opposes the
contrastive objective; it is logged, not acted on, and is worth watching across training.

**Run configuration.** `~/e0_run/d1_v1`, seeds 42/43/44 × arms `programme_only`/`programme_free`,
40 epochs, token budget 8192, programme head dim 256, lr 2e-4, weight decay 1e-2, decorrelation 0.04,
variance 0.01, separation 0.01, warmup 4, `--restrict-to-split`, no MLP-CLIP anchor or teacher in
either arm, three runs concurrent. `D1_PAIR_MANIFEST.json` records the configuration; `run_d1` asserts
the two arms' argv differ in nothing but `--objective-profile` and the output directory.

**Still outstanding.** Training (40 epochs × 6), artifact export, CALIBRA G4 controls, the paired
patient-and-cancer bootstrap, and then the readout: `d2_compare --label-a programme_only --label-b
programme_free --experiment D1`, the stratified `--target-groups heldout_pathway immune_tme
tumour_state` contrast, and the `random_control` negative control, with the six Stage-1 audit checks
applied. **No D1 number may be quoted until CALIBRA's controls and that audit have passed** — the
same discipline D2 was held to, and for the same reason.

### In plain terms

The training job is running. The health check that has blocked this experiment through four separate
defects now passes inside the real job, on the arm and seed that failed most recently, and the first
epoch of actual training already shows the objective doing something rather than sitting at the
guessing score.

Nothing scientific is claimed yet. The runs have to finish and pass their own controls first.

### Meaning for the claim

The arm `PHASE1B_TARGETED_READOUT.md` says exists nowhere on disk — a biology head trained without
programme supervision — is being produced for the first time. That is a precondition for P2's primary
evidence and for P4, and it is now in flight rather than blocked.

### Files / commits

- `bbde572`, `897c8da`
- `~/e0_run/d1_v1/` (persistent storage), `~/e0_run/d1_v1_launch.log`
